#include "program.h"
using namespace std; // подключение пространства имён стандартной библиотеки, чтобы можно было использовать функции из неё без явного указания
int main (int argc, char** args)
{
cout << "Hello, World!" << endl;
return SUCCESS;
}
